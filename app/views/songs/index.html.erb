<ul id="song-list-index">
  <% @songs.each do |song| %>
    <%= link_to song_path(song.id) do %>
      <li class="song-list-item"><%= song.artist.name %>: <%= song.title %></li> 
    <% end %>
  <% end %> 
</ul>

